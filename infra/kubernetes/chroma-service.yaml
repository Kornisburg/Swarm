apiVersion: v1
kind: Service
metadata:
  name: chroma-service
  namespace: hive
  labels:
    app: chroma
spec:
  selector:
    app: chroma
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
